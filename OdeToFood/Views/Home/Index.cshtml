@model IEnumerable<OdeToFood.Models.RestaurantListViewModel>

@{
    ViewBag.Title = "Home Page";
}

<form method="get" action="@Url.Action("Index")" data-otf-ajax="true" data-otf-targer="#restaurantList">
    <input type="search" name="searchTerm" class="searchInput"/>
    <br />
    <input type="submit" value="Search By Name" />
</form>

@*@using(Ajax.BeginForm(
        new AjaxOptions
        {
            HttpMethod="get",
            InsertionMode=InsertionMode.Replace,
            UpdateTargetId = "restaurantList"}))
    {
        <div class="searchInputBlock">
            <input type="search" name="searchTerm" class="searchInput" />
            <br />
            <input type="submit" value="Search By Name" />
        </div>
    }*@  

@Html.Partial("_Restaurants", Model)
